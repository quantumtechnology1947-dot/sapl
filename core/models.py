"""
Core models for Cortex ERP

This module uses Django's built-in User model.
No custom models are defined here as per project requirements.
"""
from django.db import models

# Using Django's built-in User model from django.contrib.auth.models
# No custom User model or other models defined in this app
